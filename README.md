This project contains a main, sample Home page that has been implemented using ReactJS and CSS.
The project showcases React helmet functionalities and is structured using React components.
There are 3 Components, namely: Courses, About and Login.
Courses contains a sample dataset as the database for the user to filter and search through.
Login simply has a UI for login- it is not functional since there is a hard-coded dataset to work with.
